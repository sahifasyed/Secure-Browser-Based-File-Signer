FROM nginx:alpine

COPY nginx.conf /etc/nginx/nginx.conf

#TODO - Set this up to use TLS / certs
# Example: Create a directory for certificates and copy them (you'd need to provide these files)

# RUN mkdir -p /etc/nginx/certs
# COPY certs/nginx.crt /etc/nginx/certs/nginx.crt
# COPY certs/nginx.key /etc/nginx/certs/nginx.key}